public class BubbleSort {
    public static void main(String[] args) {
        int[] a ={12,5,6,87,14,59};
        System.out.println("排列前的数据是：" );
       for (int x:a
             ) {
            System.out.print(" " + x);
           for (int i = 0; i < a.length - 1; i++) {
               for(int j =0; j < a.length - 1 - i;j++){

               }

           }
        }
    }

}
